@* @model EcommerceSystem.Models.Profile *@
@model UserViewModel
@{
    ViewData["Title"] = "Edit Profile";
    
}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/style.css" asp-append-version="true">
    <!-- Boxicons -->
    <link href='https://unpkg.com/boxicons@2.0.9/css/boxicons.min.css' rel='stylesheet'>
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.9.2/html2pdf.bundle.js"></script>

    <!-- Font Awesome (for the arrow icon) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">


    <!-- Boxicons (for the password toggle icons) -->
    <link href="https://cdn.jsdelivr.net/npm/boxicons@2.1.2/css/boxicons.min.css" rel="stylesheet">




    <title>Gizmode POS System</title>


    <!-- My CSS -->
    <style>
        a {
            text-decoration: none;
            font-size: 16px;
        }

            a:hover {
                /* /text-decoration: underline; /* Adds an underline only on hover */ */ color: #007bff; /* Changes color on hover */
            }

        .side-menu a {
            font-size: 1rem; /* Adjust to match your design */
            text-decoration: none;
        }

        .welcome-message {
            position: absolute;
            top: 20px;
            right: 30px;
            font-size: 18px;
            color: #954bd6;
            font-weight: 500;
        }
        /* Change icon color */
        /* .toggle-password i { */
        /* 	color: #954bd6; /* Change to any color you prefer */ */
        /* } */
        .toggle-password i {
            color: #555; /* Dark gray */
            font-size: 1.2rem;
        }


        /* Optionally, change the size of the icon */
        .toggle-password i {
            font-size: 20px; /* Adjust the size if necessary */
        }

        /* Optional: Change color on hover */
        .toggle-password:hover i {
            color: #007bff; /* Color when hovering over the icon */
        }

        .toggle-password {
            position: absolute;
            top: 50%; /* Center vertically */
            right: 15px; /* Space from the right */
            transform: translateY(-50%);
            cursor: pointer;
            color: #888; /* Icon color */
        }

        /* Style for Success Message */
        .alert-success {
            background-color: #d4edda;
            color: #155724;
            border-color: #c3e6cb;
            padding: 15px;
            border-radius: 5px;
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            box-shadow: 0 0 10px rgba(0, 255, 0, 0.2); /* Optional shadow for emphasis */
        }

            .alert-success i {
                margin-right: 10px;
                font-size: 1.3rem;
                color: #155724;
            }

        /* Style for Error Message */
        .alert-danger {
            background-color: #f8d7da;
            color: #721c24;
            border-color: #f5c6cb;
            padding: 15px;
            border-radius: 5px;
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            box-shadow: 0 0 10px rgba(255, 0, 0, 0.2); /* Optional shadow for emphasis */
        }

            .alert-danger i {
                margin-right: 10px;
                font-size: 1.3rem;
                color: #721c24;
            }

        /* Optional: Style the message box container to center it */
        .alert {
            max-width: 600px;
            margin: 0 auto;
            display: flex;
            align-items: center;
        }
    </style>
</head>


<div class="fade-in">
  

    <!-- Page Header with Go Back Icon -->
    <div class="d-flex align-items-center mb-4" style="width: 1000px; margin: 20px 0 0 26px;">
        <a asp-controller="Home"asp-action="CustomerIndex" class="text-decoration-none me-3 text-dark" title="Go Back">
            <i class="fa-solid fa-arrow-left fs-3"></i>
        </a>
        @* <a href="javascript:history.back()" class="text-decoration-none" title="Go Back"> *@
        @*     <i class="fa-solid fa-arrow-left fs-3 text-dark"></i> *@
        @* </a> *@
        <h2 class="mb-0 fw-bold" style="font-size: 1.8rem; color: #333;">Edit Profile</h2>
    </div>
        @{
            var successMessage = TempData["SuccessMessage"];
            var errorMessage = TempData["ErrorMessage"];
        }
        @if (ViewData["ErrorMessage"] != null)
        {
            <div class="alert alert-danger mt-3 text-center">
                @ViewData["ErrorMessage"]
            </div>
        }

        @if (ViewData["SuccessMessage"] != null)
        {
            <div class="alert alert-success mt-3 text-center">
                @ViewData["SuccessMessage"]
            </div>
        }
    <!-- Centered Form in a Card -->
    <div class="container d-flex justify-content-center" style="margin-bottom: 80px;">
        <div class="card shadow-sm p-4" style="width: 100%; max-width: 750px; border-radius: 12px; background-color: #ffffff;">
            <div class="card-body">
                <form asp-action="EditProfile" method="post">
                    <input type="hidden" asp-for="Id" />

                    <!-- Full Name -->
                    @* <div class="form-group mb-4"> *@
                    @*     <p style="font-size: 1.1rem; color: #555;"><strong>Full Name: </strong>@Context.Session.GetString("UserFullName")</p> *@
                    @*     <input type="hidden" name="FullName" /> *@
                    @* </div> *@
                    <div class="form-group mb-4">
                        <label asp-for="UserName" class="form-label">Username</label>
                        <input asp-for="UserName" class="form-control" value="@Model.UserName" required />
                    </div>
                    <!-- Email -->
                    <div class="form-group mb-4">
                        <label asp-for="Email" class="form-label">Email</label>
                        <input asp-for="Email" type="email" class="form-control" value="@Model.Email" required />
                        @* <label for="Email" class="fw-semibold" style="font-size: 1.1rem; color: #555;">Email</label> *@
                        @* <input type="email" class="form-control form-control-lg" id="Email" name="Email" value="@Model.Email" required> *@
                    </div>
                 
                    <!-- Phone Number -->
                    <div class="form-group mb-4">
                        <label asp-for="PhoneNumber" class="form-label">Phone Number</label>
                        <input asp-for="PhoneNumber" type="tel" class="form-control" value="@Model.PhoneNumber" />
                        @* <label for="PhoneNumber" class="fw-semibold" style="font-size: 1.1rem; color: #555;">Phone Number</label> *@
                        @* <input type="tel" class="form-control form-control-lg" id="PhoneNumber" name="PhoneNumber" value="@Model.PhoneNumber"> *@
                    </div>

                    <!-- Address -->
                    <div class="form-group mb-4">
                        <label asp-for="Address" class="form-label">Address</label>
                        <textarea asp-for="Address" class="form-control" value="@Model.Address"></textarea>
                        @* <label for="Address" class="fw-semibold" style="font-size: 1.1rem; color: #555;">Address</label> *@
                        @* <input type="text" class="form-control form-control-lg" id="Address" name="Address" value="@Model.Address"> *@
                    </div>

                    @* <div class="form-group mb-4"> *@
                    @*     <label asp-for="Password" class="form-label">New Password (Optional)</label> *@
                    @*     <input asp-for="Password" type="password" class="form-control" /> *@
                    @* </div> *@
                    <!-- Password Field -->
                    <div class="form-group mb-4" style="position: relative;">
                        <label asp-for="Password" class="fw-semibold" style="font-size: 1.1rem; color: #555;">Password</label>
                        <input asp-for="Password" type="password" class="form-control form-control-lg" id="Password" name="Password">
                        <span class="toggle-password" onclick="togglePasswordVisibility('Password', this)"
                              title="Show/Hide Password"
                              style="position: absolute; top: 50%; right: 15px; transform: translateY(-50%); cursor: pointer;">
                            <i class="bx bx-hide" style="color: #555; font-size: 1.2rem;"></i>
                        </span>
                        <small class="form-text text-muted">Leave blank if you don't want to change the password.</small>
                    </div>
                    <!-- Submit Button -->
                    <div class="text-center">
                        <button type="submit" class="btn btn-success btn-lg px-5" style="border-radius: 25px; font-size: 1.1rem;">
                            <i class="fa-solid fa-check-circle me-2"></i> Save Changes
                        </button>
                    </div>

                    <!-- Cancel Button -->
                    <div class="text-center mt-3">
                        <a asp-controller="Home" asp-action="CustomerIndex" class="btn btn-secondary btn-lg px-5" style="border-radius: 25px; font-size: 1.1rem;">
                            <i class="fa-solid fa-times-circle me-2"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@if (ViewData["ErrorMessage"] != null)
{
    <div class="alert alert-danger mt-3 text-center">
        @ViewData["ErrorMessage"]
    </div>
}

@if (ViewData["SuccessMessage"] != null)
{
    <div class="alert alert-success mt-3 text-center">
        @ViewData["SuccessMessage"]
    </div>
}


<!-- Fade-In Animation Script -->
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const fadeInElements = document.querySelectorAll('.fade-in');
        fadeInElements.forEach(el => {
            el.style.opacity = 0;
            el.style.transform = 'translateY(10px)';
            setTimeout(() => {
                el.style.transition = 'opacity 0.8s ease-in-out, transform 0.8s ease-in-out';
                el.style.opacity = 1;
                el.style.transform = 'translateY(0)';
            }, 100);
        });
    });

    // Show/Hide Password
        function togglePasswordVisibility(fieldId, toggleIcon) {
            const passwordField = document.getElementById(fieldId);
            const icon = toggleIcon.querySelector("i");
            if (passwordField.type === "password") {
                passwordField.type = "text";
                icon.classList.remove("bx-hide");
                icon.classList.add("bx-show");
            } else {
                passwordField.type = "password";
                icon.classList.remove("bx-show");
                icon.classList.add("bx-hide");
            }
        }
</script>

<!-- Custom Styling -->
<style>
    body {
        background-color: #f4f6f9;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        width: auto 1000px;
        margin-bottom: 50px;
    }

    h2 {
        color: #495057;
    }

    .card {
        border: none;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .form-control {
        border: 1px solid #ced4da;
        border-radius: 8px;
        font-size: 1rem;
    }

    .form-control:focus {
        box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
        border-color: #80bdff;
    }

    .btn-success {
        background-color: #28a745;
        border: none;
        transition: all 0.3s ease;
    }

    .btn-success:hover {
        background-color: #218838;
        transform: translateY(-2px);
    }

    .fade-in {
        opacity: 0;
        transform: translateY(10px);
    }

    .fw-semibold {
        font-weight: 600;
    }
</style>
